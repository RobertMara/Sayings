<%= form_with(model: sayer, local: true) do |form| %>
  <% if sayer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sayer.errors.count, "error") %> prohibited this sayer from being saved:</h2>

      <ul>
      <% sayer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
